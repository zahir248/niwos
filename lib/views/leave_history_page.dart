import 'package:flutter/material.dart';

class ViewLeaveRequestHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFF004AAD),
        title: Text('Leave Request History'),
      ),
      body: Center(
        child: Text('View Leave Request History Page'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class SubmitLeaveRequestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFF004AAD),
        title: Text('Leave Request'),
      ),
      body: Center(
        child: Text('Submit Leave Request Page'),
      ),
    );
  }
}
